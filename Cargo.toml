[package]
name = "did_server"
version = "0.1.0"
edition = "2021"

# 의존성 추가
[dependencies]
warp = "0.3"
tokio = { version = "1", features = ["full"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
